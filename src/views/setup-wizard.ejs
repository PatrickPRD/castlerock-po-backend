<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    }

    .setup-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 700px;
      width: 100%;
    }

    .setup-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 2.5rem;
      border-radius: 12px 12px 0 0;
      text-align: center;
    }

    .setup-header h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
      font-weight: 700;
    }

    .setup-header p {
      margin-bottom: 0;
      opacity: 0.95;
    }

    .setup-body {
      padding: 2.5rem;
    }

    .progress-container {
      margin-bottom: 2rem;
    }

    .step-badge {
      display: inline-block;
      background: #667eea;
      color: white;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      line-height: 40px;
      text-align: center;
      font-weight: 700;
      margin-bottom: 1rem;
      font-size: 0.9rem;
    }

    .step-title {
      font-size: 1.4rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #333;
    }

    .step-description {
      color: #666;
      margin-bottom: 1.5rem;
      font-size: 0.95rem;
    }

    .wizard-step {
      display: none;
    }

    .wizard-step.active {
      display: block;
      animation: fadeIn 0.3s ease-in;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      font-weight: 600;
      color: #333;
      margin-bottom: 0.5rem;
      display: block;
      font-size: 0.95rem;
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 0.75rem;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
      outline: none;
    }

    .form-section-title {
      font-size: 1.1rem;
      font-weight: 700;
      color: #667eea;
      border-bottom: 2px solid #f0f0f0;
      padding-bottom: 0.75rem;
      margin-bottom: 1.5rem;
      margin-top: 1.5rem;
    }

    .form-section-title:first-child {
      margin-top: 0;
    }

    .color-picker-group {
      display: flex;
      gap: 1rem;
      align-items: flex-end;
    }

    .color-picker-group input[type="color"] {
      width: 60px;
      height: 44px;
      padding: 2px;
      cursor: pointer;
    }

    .color-picker-group input[type="text"] {
      flex: 1;
    }

    .info-box {
      background: #f8f9ff;
      border-left: 4px solid #667eea;
      padding: 1rem;
      border-radius: 4px;
      margin-bottom: 1.5rem;
      color: #555;
      font-size: 0.95rem;
    }

    .warning-box {
      background: #fff8e1;
      border-left: 4px solid #ffc107;
      padding: 1rem;
      border-radius: 4px;
      margin-bottom: 1.5rem;
      color: #856404;
    }

    .setup-actions {
      display: flex;
      gap: 1rem;
      justify-content: space-between;
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid #eee;
    }

    .btn-setup {
      padding: 0.75rem 1.75rem;
      border-radius: 6px;
      font-weight: 600;
      font-size: 0.95rem;
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn-primary-setup {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .btn-primary-setup:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
    }

    .btn-primary-setup:disabled {
      opacity: 0.6;
      cursor: not-allowed;
      transform: none;
    }

    .btn-secondary-setup {
      background: #f5f5f5;
      color: #333;
    }

    .btn-secondary-setup:hover {
      background: #e0e0e0;
    }

    .loading-spinner {
      display: none;
      text-align: center;
      padding: 2rem;
    }

    .spinner {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #667eea;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 0 auto 1rem;
    }

    @keyframes spin {
      0% {
        transform: rotate(0deg);
      }
      100% {
        transform: rotate(360deg);
      }
    }

    .success-message {
      display: none;
      background: #d4edda;
      border: 1px solid #c3e6cb;
      color: #155724;
      padding: 1.5rem;
      border-radius: 6px;
      text-align: center;
    }

    .error-message {
      display: none;
      background: #f8d7da;
      border: 1px solid #f5c6cb;
      color: #721c24;
      padding: 1.5rem;
      border-radius: 6px;
      margin-bottom: 1.5rem;
    }

    .step-counter {
      color: #999;
      font-size: 0.9rem;
      margin-top: 1rem;
    }

    .required-asterisk {
      color: #e74c3c;
    }

    .preview-box {
      border: 1px solid #d1d5db;
      border-radius: 8px;
      padding: 12px;
      background: #f9fafb;
      margin-top: 1.5rem;
    }

    .preview-label {
      font-size: 0.9rem;
      font-weight: 600;
      color: #666;
      margin-bottom: 0.75rem;
      display: block;
    }

    .preview-bar {
      min-height: 56px;
      border-radius: 6px;
      padding: 8px 14px;
      display: flex;
      align-items: center;
      background: #212529;
      transition: background-color 0.3s ease;
    }

    #wizardPreviewImage {
      height: 34px;
      width: auto;
      object-fit: contain;
    }

    #wizardPreviewText {
      color: #fff;
      font-weight: 600;
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <div class="setup-container">
    <div class="setup-header">
      <h1>🚀 Welcome to Castlerock Cost Tracker</h1>
      <p>Let's set up your system for the first time</p>
    </div>

    <div class="setup-body">
      <!-- Error Message -->
      <div id="errorMessage" class="error-message"></div>

      <!-- Success Message -->
      <div id="successMessage" class="success-message">
        <h4>✅ Setup Complete!</h4>
        <p>Your system is ready to use. Redirecting you to the dashboard...</p>
      </div>

      <!-- Loading Spinner -->
      <div id="loadingSpinner" class="loading-spinner">
        <div class="spinner"></div>
        <p>Setting up your system...</p>
      </div>

      <!-- Wizard Form -->
      <form id="setupForm" style="display: block;">
        <!-- Step 1: Admin User -->
        <div id="step1" class="wizard-step active">
          <div class="step-badge">1</div>
          <h2 class="step-title">Create Admin Account</h2>
          <p class="step-description">Set up your super admin user account to manage the system</p>

          <div class="form-group">
            <label for="adminFirstName">First Name <span class="required-asterisk">*</span></label>
            <input type="text" id="adminFirstName" name="adminFirstName" required placeholder="e.g., John">
          </div>

          <div class="form-group">
            <label for="adminLastName">Last Name <span class="required-asterisk">*</span></label>
            <input type="text" id="adminLastName" name="adminLastName" required placeholder="e.g., Doe">
          </div>

          <div class="form-group">
            <label for="adminEmail">Email <span class="required-asterisk">*</span></label>
            <input type="email" id="adminEmail" name="adminEmail" required placeholder="e.g., admin@example.com">
          </div>

          <div class="form-group">
            <label for="adminPassword">Password <span class="required-asterisk">*</span></label>
            <input type="password" id="adminPassword" name="adminPassword" required placeholder="Create a strong password">
            <small class="text-muted">Minimum 8 characters recommended</small>
          </div>

          <div class="form-group">
            <label for="adminPasswordConfirm">Confirm Password <span class="required-asterisk">*</span></label>
            <input type="password" id="adminPasswordConfirm" name="adminPasswordConfirm" required placeholder="Confirm your password">
          </div>

          <div class="step-counter">Step <strong>1</strong> of 5</div>
        </div>

        <!-- Step 2: Site -->
        <div id="step2" class="wizard-step">
          <div class="step-badge">2</div>
          <h2 class="step-title">Create Your First Site</h2>
          <p class="step-description">A site represents your main project or location</p>

          <div class="form-group">
            <label for="siteName">Site Name <span class="required-asterisk">*</span></label>
            <input type="text" id="siteName" name="siteName" required placeholder="e.g., Main Office Building">
          </div>

          <div class="form-group">
            <label for="siteDescription">Description</label>
            <textarea id="siteDescription" name="siteDescription" rows="3" placeholder="Optional description of the site"></textarea>
          </div>

          <div class="step-counter">Step <strong>2</strong> of 5</div>
        </div>

        <!-- Step 3: Location -->
        <div id="step3" class="wizard-step">
          <div class="step-badge">3</div>
          <h2 class="step-title">Add a Location</h2>
          <p class="step-description">Locations are houses or parts of the site you want to assign costs to</p>

          <div class="form-group">
            <label for="locationName">Location Name <span class="required-asterisk">*</span></label>
            <input type="text" id="locationName" name="locationName" required placeholder="e.g., Ground Floor">
          </div>

          <div class="form-group">
            <label for="locationAddress">Address</label>
            <input type="text" id="locationAddress" name="locationAddress" placeholder="Optional address">
          </div>

          <div class="form-group">
            <label for="locationDescription">Description</label>
            <textarea id="locationDescription" name="locationDescription" rows="3" placeholder="Optional description"></textarea>
          </div>

          <div class="step-counter">Step <strong>3</strong> of 5</div>
        </div>

        <!-- Step 4: Stage -->
        <div id="step4" class="wizard-step">
          <div class="step-badge">4</div>
          <h2 class="step-title">Create a Stage</h2>
          <p class="step-description">Stages represent phases of work (e.g., Groundworks, Plastering, Plumbing)</p>

          <div class="form-group">
            <label for="stageName">Stage Name <span class="required-asterisk">*</span></label>
            <input type="text" id="stageName" name="stageName" required placeholder="e.g., Foundation Work">
          </div>

          <div class="info-box">
            <strong>Tip:</strong> You can add more stages later from the application's Stage management page.
          </div>

          <div class="step-counter">Step <strong>4</strong> of 5</div>
        </div>

        <!-- Step 5: Worker & Settings -->
        <div id="step5" class="wizard-step">
          <div class="step-badge">5</div>
          <h2 class="step-title">Add Your First Worker & Branding</h2>
          <p class="step-description">Complete your setup with a worker and customize your branding</p>

          <div class="form-section-title">Worker Information</div>

          <div class="form-group">
            <label for="workerFirstName">Worker First Name <span class="required-asterisk">*</span></label>
            <input type="text" id="workerFirstName" name="workerFirstName" required placeholder="e.g., Michael">
          </div>

          <div class="form-group">
            <label for="workerLastName">Worker Last Name <span class="required-asterisk">*</span></label>
            <input type="text" id="workerLastName" name="workerLastName" required placeholder="e.g., Smith">
          </div>

          <div class="form-group">
            <label for="workerEmail">Worker Email</label>
            <input type="email" id="workerEmail" name="workerEmail" placeholder="Optional email address">
          </div>

          <div class="form-group">
            <label for="workerPhone">Worker Phone</label>
            <input type="tel" id="workerPhone" name="workerPhone" placeholder="Optional phone number">
          </div>

          <div class="form-section-title">Company Details</div>

          <div class="form-group">
            <label for="companyName">Company Name</label>
            <input type="text" id="companyName" name="companyName" value="Castlerock Homes" placeholder="Your company name">
          </div>

          <div class="form-group">
            <label for="companyEmail">Company Email</label>
            <input type="email" id="companyEmail" name="companyEmail" placeholder="e.g., info@company.ie">
          </div>

          <div class="form-group">
            <label for="companyPhone">Company Phone</label>
            <input type="tel" id="companyPhone" name="companyPhone" placeholder="e.g., +353 (0)1 XXX XXXX">
          </div>

          <div class="form-section-title">Branding</div>

          <div class="form-group">
            <label for="headerColor">Header Color</label>
            <div class="color-picker-group">
              <input type="color" id="headerColor" name="headerColor" value="#212529">
              <input type="text" id="headerColorHex" name="headerColorHex" value="#212529" placeholder="#212529">
            </div>
          </div>

          <div class="form-group">
            <label for="accentColor">Accent Color</label>
            <div class="color-picker-group">
              <input type="color" id="accentColor" name="accentColor" value="#1e40af">
              <input type="text" id="accentColorHex" name="accentColorHex" value="#1e40af" placeholder="#1e40af">
            </div>
          </div>

          <div class="form-group">
            <label for="logoFile">Upload Company Logo (Optional)</label>
            <input type="file" id="logoFile" name="logoFile" accept="image/png,image/jpeg,image/jpg,image/webp,image/svg+xml">
            <small class="text-muted">Accepted formats: PNG, JPG, WEBP, SVG. Max size 2 MB. You can skip this and add it later from Application Settings.</small>
          </div>

          <div class="form-group">
            <label for="faviconFile">Upload Favicon (Optional)</label>
            <input type="file" id="faviconFile" name="faviconFile" accept="image/x-icon,image/png,image/svg+xml">
            <small class="text-muted">Accepted formats: ICO, PNG, SVG. Recommended size: 32x32px or 64x64px. Max size 500 KB.</small>
          </div>

          <!-- Header Preview -->
          <div class="preview-box">
            <span class="preview-label">Header Preview:</span>
            <div id="wizardPreviewBar" class="preview-bar">
              <img id="wizardPreviewImage" src="/assets/Logo.png" alt="Logo preview" />
              <span id="wizardPreviewText" style="display: none;">Castlerock Homes</span>
            </div>
          </div>

          <div class="step-counter">Step <strong>5</strong> of 5 (Final Step)</div>
        </div>
      </form>

      <!-- Navigation Buttons -->
      <div class="setup-actions">
        <button type="button" id="prevBtn" class="btn-setup btn-secondary-setup d-none">← Back</button>
        <button type="button" id="nextBtn" class="btn-setup btn-primary-setup">Next →</button>
      </div>
    </div>
  </div>

  <script src="setup-wizard.js"></script>
</body>
</html>
